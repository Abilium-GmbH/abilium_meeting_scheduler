<odoo>
  <data>

    <!-- explicit list view definition -->
    <record model="ir.ui.view" id="meeting_scheduler.list">
      <field name="name">meeting_scheduler list</field>
      <field name="model">meeting_scheduler</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>

    <!-- actions opening views on models -->
    <record model="ir.actions.act_window" id="meeting_scheduler.action_window">
      <field name="name">meeting_scheduler window</field>
      <field name="res_model">meeting_scheduler</field>
      <field name="view_mode">tree,form</field>
    </record>

    <!-- server action to the one above -->
    <record model="ir.actions.server" id="meeting_scheduler.action_server">
      <field name="name">meeting_scheduler server</field>
      <field name="model_id" ref="model_meeting_scheduler"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>

    <!-- Top menu item -->
    <menuitem name="Abilium Meeting Scheduler" id="meeting_scheduler.menu_root"/>

    <!-- menu categories -->
    <menuitem name="Menu 1" id="meeting_scheduler.menu_1" parent="meeting_scheduler.menu_root"/>
    <menuitem name="Menu 2" id="meeting_scheduler.menu_2" parent="meeting_scheduler.menu_root"/>

    <!-- actions -->
    <menuitem name="List" id="meeting_scheduler.menu_1_list" parent="meeting_scheduler.menu_1"
              action="meeting_scheduler.action_window"/>
    <menuitem name="Server to list" id="meeting_scheduler" parent="meeting_scheduler.menu_2"
              action="meeting_scheduler.action_server"/>

  </data>
</odoo>